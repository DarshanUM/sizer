---
kind: "Route"
apiVersion: "v1"
metadata:
  name: "analytics-grafana-test"
  labels:
     component: "analytics-grafana-test"
  annotations: 
    haproxy.router.openshift.io/timeout: 60s
spec:
   host: "hyperflexsizer-analytics.cisco.com"
   to:
     kind: "Service"
     name: "analytics-grafana-svc"
   port:
     targetPort: "web"
   tls:
     insecureEdgeTerminationPolicy: Allow
     termination: edge
